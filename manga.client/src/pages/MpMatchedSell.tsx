import React from 'react';
import { Box, Typography } from '@mui/material';
import axios from 'axios';

const MpMatchedSell: React.FC = () => {

  return (
    <Box>
      <Typography variant="h5">テスト</Typography>
    </Box>
  );
};

export default MpMatchedSell;
